type Author {
  id: ID!
  name: String!
 }
 
type Post {
  id: ID!
  title: String!
  body: String
  createdBy: Author!
}

 type Comment {
  id: ID!
  authorId: String!
  postId: String!
  text: String
 }

schema {
  query: Query
}

type Query {
  allPosts: [Post]
}
